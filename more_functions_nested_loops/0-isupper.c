#include "main.h"
#include <stdio.h>

/**
 * main - checks for uppercase character
 *
 * Return: Always 0.
 */
int _isupper(int c)
{
    if (c >= 'a' && c <= 'z')
    {
        return (1)
    }
    
    return (0);
}